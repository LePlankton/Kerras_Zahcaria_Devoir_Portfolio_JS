function Header() {         // On crée une fonction qui s'appelle Header, c'est un composant React qui va retourner du JSX
  return (                  // Cette fonction retourne du JSX (HTML-like syntax) qui sera rendu dans le DOM
    <div>Header</div>       // Le contenu affiché à l'écran
  )
}

export default Header       // On exporte le composant Header pour pouvoir l'utiliser dans d'autres fichiers de notre application React
